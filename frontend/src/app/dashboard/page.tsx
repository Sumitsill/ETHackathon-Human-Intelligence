"use client";

import AppLayout from '@/app/app/layout';
import CommandCenterDashboard from '@/app/app/page';

export default function DashboardRoute() {
  return (
    <AppLayout>
      <CommandCenterDashboard />
    </AppLayout>
  );
}
